language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
  - "3.9"
  - "nightly"  # nightly build
# Command to install dependencies
# Include also `test` dependencies
install:
  - pip install '.[test]'
# Command to run tests
script:
  - pytest
